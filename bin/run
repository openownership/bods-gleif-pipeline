#!/usr/bin/env python3
from pathlib import Path

def pipeline():
    Path('data').mkdir(exist_ok=True)
    from bodspipelines.pipelines import gleif
    gleif.pipeline.process("ingest")

if __name__ == "__main__":
    pipeline()
